export default function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "48px" }}>
      <h1>Codvly Web</h1>
      <p>
        The web build of the Codvly workspace is a placeholder — the desktop
        app (Tauri) is the current shell. Both share the same UI and core
        packages.
      </p>
    </main>
  );
}
